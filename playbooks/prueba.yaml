- hosts: all
  order: inventory
  vars: {}
  gather_facts: True
  
  tasks:
    - name: tarea 1
      debug:
        msg: soy la primera tarea en ejecución por {{ nombre }} que tiene {{ edad }} años
  